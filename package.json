{
  "name": "openlore",
  "version": "0.2.0",
  "private": true,
  "workspaces": [
    "shared/pack/js",
    "game/client",
    "studio/app"
  ],
  "scripts": {
    "pack:build": "yarn workspace @openlore/pack build",
    "game:client": "yarn workspace @openlore/client dev",
    "studio:app": "yarn workspace @openlore/studio dev",
    "build": "yarn pack:build && yarn workspace @openlore/client build"
  },
  "dependencies": {
    "@bufbuild/protobuf": "^2.11.0",
    "ecc-universal": "^1.8.0",
    "openai": "^6.27.0"
  },
  "devDependencies": {
    "@bufbuild/buf": "^1.66.1",
    "@bufbuild/protoc-gen-es": "^2.11.0"
  }
}
